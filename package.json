{
  "name": "lp-syntia",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page Syntia by Syntegra",
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
